Week 4: Tool-Schema Audit and Citation Validation

Date: 25 September 2026

Deliverable: Updated acceptance criteria for US-8 in docs/requirements/Project_charter and user stories.docx, plus src/orchestrator/validation.py, with its supporting audit document (docs/requirements/week4-tool-schema-user-story-audit.md), unit tests (tests/test_validation.py) and demonstration script (scripts/member1_validation_smoke.py).

What I asked the AI for: I asked it to check whether the tool-related user stories (US-1 through US-11) actually match the real tool schemas, using Andrew's draft schemas, Mustafa's dispatcher code and Mustafa's Week 4 report as the ground truth, and to flag anywhere the acceptance criteria said something the code did not actually do. Separately, I asked it to write the code Mustafa's report itself calls out as missing: something that automatically rejects a model proposal before dispatch if it has no citation or cites a source that was never supplied. I also asked it to correct the repository path on the task sheet (src/orchestrator/), since the real folder Mustafa built is src/orchestrator/, the same kind of mismatch we resolved in Week 3 for the knowledge folder.

What the AI produced: A story-by-story comparison of the eight tool-related stories against the schemas, confirming seven matched and finding one that did not: US-8's wording only covered test proposals, when the actual requirement (per the propose_action prompt and Mustafa's own report) applies to every proposal the model can make. It widened US-8's acceptance criteria accordingly and applied that edit directly to the charter document. It also wrote src/orchestrator/validation.py (one function, validate_citations), eight unit tests including two run against the team's real fabricated-citation fixture, and a three-scenario demonstration script.

Verification I performed myself: I ran the test suite and the demonstration script myself in VS Code's terminal, not just accepted the AI's own claim that they passed. python -m unittest tests.test_validation -v returned all 8 new tests passing. Running it again together with Mustafa's existing dispatcher tests, tests.test_validation tests.integration.test_tool_dispatcher -v, returned all 24 passing, confirming the new file does not break his code. python scripts\member1_validation_smoke.py ran and printed the three scenarios (fabricated citation rejected, clean proposal executed, missing citation rejected) exactly as claimed. I also opened the rendered charter document myself to check the US-8 amendment reads correctly in context and did not disturb the rest of the table.

What I changed or would still change myself: I asked for the file to be moved from the task sheet's src/orchestrator/ path into the real src/orchestrator/ folder rather than creating an unused duplicate folder, the same correction I made in Week 3. I still want Mustafa and Andrew to weigh in before this is considered final: the audit used Andrew's Week 1 draft tool schemas because no updated Week 4 version exists yet, and I want it re-checked once he registers the real tools with their actual risk levels and roles.

Not AI-generated: the decision to widen US-8 rather than add a thirteenth story, and to leave the US-11 and role-modelling gaps as open items instead of rewriting them, was mine, following the same approach the team already used for US-11 in Week 2. The choice to correct the folder path rather than follow the task sheet literally was also mine. Running and reading the verification output myself, not just trusting the AI's summary of it, was mine.

Data sent to the AI: only the team's own repository files already in the project, source code (tool_dispatcher.py, types.py), the charter document, Mustafa's and Andrew's own Week 4/Week 1 reports, and the test fixtures already used by the evaluation harness. No API keys, credentials, or confidential data were shared.
